% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morpho_v1.R
\name{get_vault_v1_list}
\alias{get_vault_v1_list}
\title{Get a paginated list of all Vault V1s (MetaMorpho)}
\usage{
get_vault_v1_list(
  first = 100,
  skip = 0,
  chain_ids = c(1, 8453),
  order_by = "TotalAssetsUsd",
  order_direction = "Desc",
  human_readable = TRUE
)
}
\arguments{
\item{first}{Integer. Number of items to return per page (default: 100)}

\item{skip}{Integer. Number of items to skip for pagination (default: 0)}

\item{chain_ids}{Integer vector. Filter vaults by chain IDs (default: c(1, 8453))}

\item{order_by}{Character. Field to order by (default: "TotalAssetsUsd")}

\item{order_direction}{Character. "Asc" or "Desc" (default: "Desc")}

\item{human_readable}{Logical. Add formatted columns (default: TRUE)}
}
\value{
A tibble with basic vault information including TVL and correct share prices
}
\description{
Retrieves a paginated list of all Morpho Vault V1s (MetaMorpho) with basic information.
These vaults typically have the highest TVL (billions of dollars).
}
